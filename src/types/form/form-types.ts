export interface RegisterForm {
    password: string;
    passwordRepeat: string;
    email: string;
    firstName: string;
    lastName: string;
    rulesAccepted: boolean; 
    // yearsOfPlayingPiano: number;
}